/* exported getValue */
// Pseudocode
// The function getValue passes in two parameters, an object and a key representing
//   a property of that object. Our function returns the value found at the key
//   property.
// The function uses bracket notation to access the value held at the key property
//   of the object.
// The function returns this value

function getValue(object, key) {
  return object[key];
}

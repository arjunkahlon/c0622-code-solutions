/* exported head */

// Pseudocode
// The function takes in an array and returns the first element (head) of the
// array.
// The function accesses the first element at index 0 and returns it.

function head(array) {
  return array[0];
}
